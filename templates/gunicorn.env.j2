set -o allexport
DB_BASE={{db_base}}
DB_HOST={{db_host}}
DB_PASS={{db_password}}
DB_PORT={{db_port}}
DB_USER={{db_user}}
AUTH0_CLIENT_ID={{auth0_client_id}}
AUTH0_SECRET_ID={{auth0_secret_id}}
SENTRY_DSN={{sentry_dsn}}
AWS_DEFAULT_REGION={{aws_region}}
DYNAMO_REGION={{aws_region}}
POSTS_TABLE={{posts_table}}
FLAG_TABLE={{flagged_posts_table}}
EXT_POSTS_TOPIC={{external_post_sns_topic}}
UPLOAD_BUCKET={{upload_bucket}}
RESIZE_BUCKET={{resize_bucket}}
PROFILE_PIC_BUCKET={{profile_pic_bucket}}
set +o allexport
